import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: "What's in my pantry?",
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
